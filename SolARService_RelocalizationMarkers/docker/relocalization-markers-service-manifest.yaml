apiVersion: apps/v1
kind: Deployment
metadata:
  name: relocalization-markers-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: relocalization-markers-service
  template:
    metadata:
      labels:
        app: relocalization-markers-service
    spec:
      imagePullSecrets:
      - name: regcredsupra
      containers:
      - name: relocalization-markers-service
        image: solar-docker-local.artifact.b-com.com/relocalization/0.10.0/relocalization-markers-service:latest
        env:
        - name: SOLAR_LOG_LEVEL
          value: INFO
        - name: XPCF_GRPC_MAX_RECV_MSG_SIZE
          value: "7000000"
        - name: XPCF_GRPC_MAX_SEND_MSG_SIZE
          value: "20000"
---

kind: Service
apiVersion: v1
metadata:
  name: relocalization-markers-service
  labels:
    app: relocalization-markers-service
spec:
  type: NodePort
  selector:
    app: relocalization-markers-service
  ports:
  - name: http
    port: 80
    targetPort: 8080
    nodePort: 31880

---

apiVersion: v1
kind: ServiceAccount
metadata:
  name: relocalization-markers-service

---

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: relocalization-markers-service-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-admin
subjects:
- kind: ServiceAccount
  name: relocalization-markers-service
  namespace: relocalization-markers-service.artwin.b-com.com:q

---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    ingress.kubernetes.io/force-ssl-redirect: "true"
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/backend-protocol: "GRPC"
  name:
    relocalization-markers-service
spec:
  tls:
  - hosts:
      - relocalization-markers-service.artwin.b-com.com
  rules:
  - host: relocalization-markers-service.artwin.b-com.com
    http:
      paths:
      - backend:
          service:
            name: relocalization-markers-service
            port:
              number: 80
        path: /
        pathType: Prefix
